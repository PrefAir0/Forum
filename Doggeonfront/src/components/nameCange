import React from 'react'

const nameCange = () => {
  return (
    <div><TextField
            variant="outlined"
                label="Имя"
                value={name}
                onChange={(e) => setName(e.target.value)}
                required
                fullWidth
                sx={{ mb: 5, width: 300 }}
            /></div>
  )
}

export default nameCange